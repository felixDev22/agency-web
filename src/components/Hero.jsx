// eslint-disable-next-line no-unused-vars
import React from 'react';
import styles from '../constants/styles';

const Hero = () => {
  return (
    <section
      id="home"
      className={`${styles.heroWrapper} ${styles.paddingY}`}>
      Hero
    </section>
  );
};

export default Hero;
